# Server host binding (0.0.0.0 allows external connections)
HOST=0.0.0.0

# Server port number
PORT=8082

# Enable auto-reload on code changes (development only)
UVICORN_RELOAD=true

# ------------------------------------------------------------
# AWS Bedrock Configuration
# ------------------------------------------------------------
# Primary AWS credentials for Bedrock (falls back to AWS_* if not set)
BEDROCK_AWS_REGION=
BEDROCK_AWS_ACCESS_KEY_ID=
BEDROCK_AWS_SECRET_ACCESS_KEY=
STRANDS_MODEL_ID=amazon.nova-lite-v1:0 


N_ATLAS_API_BASE=https://site.modal.run/v1

OPENROUTER_API_KEY=

DATABASE_URL=sqlite:///./data/sqlite.db

FIREBASE_CREDENTIALS_PATH=
ENABLE_ACCESS_CONTROL=True
INITIAL_ACCESS_CODE=INITIAL_ACCESS_CODE